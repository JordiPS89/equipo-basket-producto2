import { JugadorFiltroPipe } from './jugador-filtro.pipe';

describe('JugadorFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new JugadorFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
